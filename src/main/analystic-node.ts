// Analytics — disabled in this fork.
// To enable, replace with your own Google Analytics 4 measurement_id and api_secret.
// Reference:
// https://developers.google.com/analytics/devguides/collection/protocol/ga4/reference

const measurement_id = ``
const api_secret = ``

export async function event(name: string, params: any = {}) {
  // Analytics disabled in fork
  return
}
